{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["path/to/prisma/schema.prisma"]
      }
    }
  ],
  "buildCommand": "npm run build && npx prisma generate"
}
